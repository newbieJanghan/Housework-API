<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.ssafy.housework.model.familyHousework.FamilyHouseworkDao">
    <select id="selectOne" resultType="Housework">
        SELECT *
        FROM houseworks
        WHERE family_id = #{familyId}
          AND id = #{id}
    </select>

    <select id="selectAll" resultType="Housework">
        SELECT *
        FROM houseworks
        WHERE family_id = #{family}
    </select>

    <select id="query" resultType="Housework">
        SELECT *
        FROM houseworks
        WHERE family_id = #{family}
        <if test="date != null">
            AND start_at >= #{date.from}
            AND due_at<= #{date.to}
        </if>
        <if test="assignedUserId != null">
            AND assigned_user_id = #{assignedUserId}
        </if>
    </select>

    <delete id="delete" parameterType="int">
        DELETE
        FROM houseworks
        WHERE family_id = #{familyId}
          AND id = #{id}
    </delete>
</mapper>